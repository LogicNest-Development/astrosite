---
const feats = [
  ['NVMe storage','Blazing fast reads/writes for your workloads.'],
  ['DDoS filtering','Network-layer protection included.'],
  ['Backups','Two automated backups included by default.'],
  ['SFTP access','Full control of your files.'],
];
---
<section class="container-section pb-12 md:pb-16 grid grid-cols-1 md:grid-cols-2 gap-6">
  {feats.map(([t,d]) => (
    <div class="rounded-2xl border border-white/10 p-6 shadow-ring">
      <h3 class="text-xl font-semibold mb-2">{t}</h3>
      <p class="text-muted">{d}</p>
    </div>
  ))}
</section>
